# Informasi

Frontend    : Vue
Backend     : Springboot
Database    : PostgreSQL


## How to run 

- Jalankan perintah `./mvnw spring-boot:run` untuk menjalankan Backend
- Masuk ke dalam folder `web` kemudian jalankan perintah `npm run dev` untuk menjalankan Frontend
- 